# Copy this file to .env and edit if needed
# This project *prefere* que você defina a variável DATABASE_URL completa
# em vez de usar DB_HOST/DB_PORT/DB_USER/DB_PASSWORD separadas.
# Exemplos (não comite credenciais reais):
# DATABASE_URL="postgresql://postgres:senha@seu-host:5432/seu_db?sslmode=require"

# Se você realmente precisa usar variáveis separadas, defina todas elas
# explicitamente. Não há valores padrão neste arquivo de exemplo.
# DB_HOST=localhost
# DB_PORT=5432
# DB_NAME=VACINACAO
# DB_USER=postgres
# DB_PASSWORD=<REPLACE_WITH_PASSWORD>

# Opcional: ajustes de pool
# DB_POOL_SIZE=5
# DB_MAX_OVERFLOW=10

# Local upload directory
UPLOAD_DIR=uploads

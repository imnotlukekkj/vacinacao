# Copy this file to .env and edit if needed
DB_HOST=db
DB_PORT=5432
DB_NAME=vacina_brasil
DB_USER=postgres
DB_PASSWORD=example
DB_HOST=localhost
DB_PORT=5432
DB_NAME=VACINACAO
DB_USER=postgres
# DB_PASSWORD should be set to your database password. Keep this secret and do NOT commit real credentials.
DB_PASSWORD=<REPLACE_WITH_PASSWORD>

# Optional: you can also provide DATABASE_URL directly instead of the DB_* vars.
# DATABASE_URL="postgresql://postgres:password@localhost:5432/VACINACAO"

# Local upload directory
UPLOAD_DIR=uploads
